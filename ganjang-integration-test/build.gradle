plugins {
    id "net.ltgt.apt" version "0.15"
    id "net.ltgt.apt-idea" version "0.15"
}

dependencies {
    annotationProcessor project(':ganjang-compiler')
    compile project(':ganjang')

    testCompile 'junit:junit:4.+'
    testCompile 'org.assertj:assertj-core:3.+'
}
